import 'package:flutter/material.dart';

class ProbabilitiesApp extends StatelessWidget {
  static const String routeName = '/probabilities-app';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
